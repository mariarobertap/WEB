import styled from "styled-components";

export const List = styled.ul`
    display: flex;
    flex-wrap: wrap;
    flex-direction: row;
    list-style: none;
    padding-left: 2em;
    padding-right: 2em;
`

export const ListItem = styled.li`
    height: fit-content;
    cursor: pointer;
`